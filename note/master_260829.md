# Note販売プロジェクト マスタードキュメント

**作成日：2026-08-29**（現状のみを載せた軽量版。**通常のセッションではこのファイルだけ読めば足りる。**各商品の詳細が必要なときだけ §6 のファイルを開く）
関連（別プロジェクト）：[Chamaru](../茶丸/統合マスター_260829.md) ／ [RouteNote / Ongaku Toshokan](../RouteNote/Ongaku%20toshokan/master20260824.md)

---

## 1. プロジェクト概要

| 項目 | 値 |
|---|---|
| 目的 | AI作業効率化ツールを作って **note.com** で有料販売する |
| 販売アカウント | **ピクト＠AI時短ラボ**（アイコン・バナー案：`Gemini 画像ダウンロード/素材/note_*_ピクト案v2.png`） |
| 現在の商品数 | 2（①販売準備ほぼ完了 ／ ②開発完了・販売準備これから） |
| 収益状況 | 未販売（note記事はまだ公開していない） |

### 運用の前提（守ること）

- 配布物の**転売・再配布は禁止**と明記する。商品にもその旨を記載済み
- 生成画像やAI出力の権利は各サービス（Google等）の規約に従う前提で案内する
- Chrome拡張はウェブストアを通さない「非公式拡張機能」としての配布（デベロッパーモード必須）。この制約を隠さずマニュアルに書く

---

## 2. 商品一覧・現在の状態（2026-08-29時点）

| # | 商品 | 形態 | 開発 | 販売素材 | 状態 |
|---|---|---|---|---|---|
| ① | Gemini画像 1クリック一括ダウンローダー | Chrome拡張（Manifest V3、v1.0.2） | ✅ 実機確認済み・ZIPビルド済み | 販売記事下書き・導入マニュアル・見出し画像候補・宣伝動画（モック）済み | **note記事の公開待ち**。有料エリアのZIPリンク差し込みと画像挿入が未 |
| ② | 一括背景透過＆自動トリミングツール | ブラウザ完結の単一HTML（ローカル、サーバー送信なし） | ✅ 動作確認済み（2026-08-29に透過アルゴリズム改良） | なし | **販売素材これから**。記事・マニュアル・宣伝画像・価格が未定 |

---

## 3. 商品① Gemini画像 一括ダウンローダー

- **リポジトリ**：`shimenawas-design/shimenawas2`　ブランチ `claude/gemini-batch-image-downloader-626pj5`　最新コミット `8adc4c1`（origin と一致）
- **できること**：Geminiのチャット画面で拡張アイコン→接頭辞入力→ボタン1つで、表示中の生成画像を `接頭辞_001.png` 形式の連番で一括保存。チャットを自動スクロールして全画像を回収
- **成果物**
  - 拡張本体：`shimenawas2/`（`manifest.json` / `content.js` / `background.js` / `popup.*` / `icons/`）
  - 配布ZIP：`shimenawas2/dist/gemini-image-bulk-downloader-v1.0.2.zip`（＝`Note/Gemini 画像ダウンロード/商材/` にも同じものあり）
  - 導入マニュアル：`shimenawas2/docs/INSTALL_README.txt`（英数字名。配布ZIP同梱）
- **技術的な既知の注意点**（詳細は `docs/HANDOVER.md` §6）
  - Geminiの生成画像 `blob:` URL は fetch/XHR 不可（CSP）。`<img>`→canvas→`toDataURL()` で取得している
  - `chrome.downloads.download()` の `filename` は `data:` URL で無視されることがある → `onDeterminingFilename` で強制上書き
  - content script はインストール前から開いていたタブには注入されない → タブ再読み込みが必要
- **残タスク**
  1. **note記事を公開する**（`Note販売記事.md` の `[ここに〜挿入]` を埋める：画像4点＋ZIPダウンロードリンク。価格想定 980円）
  2. 実写デモ動画の撮影（横型16:9推奨。手順は `shimenawas2/docs/VIDEO_DEMO_HANDOVER.md`）
  3. 複数人での配布検証（うまくいかない時は F12 の `[Gemini画像DL]` ログを回収してもらう）
  4. （任意）Chromeウェブストア公開を検討する場合は審査要件（権限説明・プライバシーポリシー）の確認が必要

---

## 4. 商品② 一括背景透過＆自動トリミングツール

- **場所**：`Note/一括背景透過ツール/index.html`（ローカルのみ。リポジトリ未登録）
- **できること**：白背景の画像を複数まとめて読み込み、ブラウザ内だけで背景透過＋自動トリミング。ZIP一括ダウンロード。画像は外部送信しない
- **仕様メモ**（2026-08-29改良後）
  - 端から連結した白だけを塗りつぶしで透過（内側の目のハイライト等は残す）＋フチの白かぶり除去
  - 白色許容度の初期値 18。チェックボックス「白フチ（ダイカット枠）も消す」を任意追加（漏れたら自動巻き戻し）
- **残タスク（販売化の前提）**
  1. 価格・商品名の確定
  2. note販売記事の作成（①の `Note販売記事.md` が雛形になる）
  3. 導入マニュアル（HTMLを開くだけなので①より簡単）
  4. 見出し画像・宣伝素材
  5. 配布形態の決定（HTML単体を渡す／zip）とリポジトリ登録の要否判断

---

## 5. 次にやること（プロジェクト優先順）

1. **商品①のnote記事を公開**（残タスク §3-1）— 収益化の最短ルート。素材はほぼ揃っている
2. 商品①の実写デモ動画（§3-2）
3. 商品②の販売準備を開始（§4 の残タスク）
4. 商品①の複数人配布検証（§3-3）

---

## 6. 参照ファイルマップ（必要なときだけ開く）

```
Note/
├── master_260829.md                         本書（現状のみ・まずこれ）
├── Note販売プロジェクト＆引き継ぎ書.pdf       商品①の見出し画像デザイン案（6枚・画像のみ）
├── Gemini 画像ダウンロード/
│   ├── HANDOVER.md                           商品①の技術引継ぎ（= shimenawas2/docs/HANDOVER.md）
│   ├── 導入マニュアル.md                      購入者向けインストール手順
│   ├── Note販売記事.md                        note入稿用の記事下書き（価格980円想定）
│   ├── 素材/                                  記事挿入画像・note アイコン/バナー案
│   └── 商材/                                  配布ZIP と展開済みソース
│       （動画の引継ぎは shimenawas2/docs/VIDEO_DEMO_HANDOVER.md）
├── 一括背景透過ツール/index.html              商品②本体
└── Youtube用/                                商品①の YouTube 用バナー・サムネ・アイコン

shimenawas2/（商品①のリポジトリ。ブランチ claude/gemini-batch-image-downloader-626pj5）
├── manifest.json / content.js / background.js / popup.*
├── dist/gemini-image-bulk-downloader-v1.0.2.zip
└── docs/
    ├── HANDOVER.md                            技術的な経緯・既知の注意点
    ├── INSTALL_README.txt                     配布ZIP同梱マニュアル
    ├── VIDEO_DEMO_HANDOVER.md                 実写デモ動画の作り方
    └── promo/mock-demo.html                   宣伝モック動画のHTML原稿
```
